[package]
name = "herdr-standup"
version = "0.3.0"
edition = "2024"
license = "MIT"

[dependencies]
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
strip = true
codegen-units = 1
panic = "abort"
